<div dir="rtl" align="right">

# מדריך למשתמש — OR Planer 📐

## מובייל (טלפון / טאבלט)

האפליקציה רצה בדפדפן — כולל **Streamlit Cloud** בכתובת הציבורית. בנייד: העלאת תמונות ועריכה עובדות; **טבלאות** עלולות לדרוש גלילה אופקית. **תצוגת PDF מוטמעת** לפעמים לא נטענת — השתמש ב**הורדה** (כבר מסומן בממשק).

## מה עושה האפליקציה?

האפליקציה מקבלת שרטוט של פרופילי אלומיניום (תמונה או קובץ JSON),
ויוצרת אוטומטית את כל הקבצים שצריך להזמנה:

- **Excel** — טבלת הזמנה מסודרת
- **PDF סיכום** — טבלה מודפסת עם כל הפנלים
- **PDF שרטוטים** — שרטוט הנדסי לכל פנל בנפרד (חתך, פריסה, מידות)
- **DXF** — קבצי שרטוט לפתיחה ב-AutoCAD

---

## שלב 1: הכנת המידות

### אפשרות א׳ — צילום שרטוט ביד (הכי קל)

צלם את השרטוט בנייד. האפליקציה תחלץ את המידות אוטומטית.

**טיפים לצילום טוב:**

- צלם על רקע בהיר (נייר לבן או משובץ)
- וודא שכל המספרים קריאים וברורים
- אל תחתוך חלק מהשרטוט — צלם את הדף המלא
- אור טוב, בלי צללים על השרטוט
- אפשר לצלם כמה דפים — העלה את כולם ביחד

**מה לכתוב בשרטוט:**

```
לכל פנל/פרופיל, כתוב:

1. מספר מזהה (למשל: S-101, או מספר כלשהו)
2. אורך במ"מ (למשל: 2300)
3. רוחב במ"מ (למשל: 270)
4. כמות (למשל: x6)
5. מידות הפרופיל — החתך של הכיפוף
6. הערות (למשל: ניקוב / בלי ניקוב / לסובב)
```

### דוגמה לשרטוט ברור:

```
S-101  x6
אורך: 1570 מ"מ

   25 ─┐
       │ 20
  170 ─┘
       │ 20
   25 ─┘

פרופיל: 25, 20, 170, 20, 25
```

**חשוב:** המספרים בפסיקים מתארים **רק את חתך הפרופיל** של אותו פנל (פנל אחד = שורה אחת). אורך, רוחב וכמות הם שדות נפרדים. אם אותו מק״ט מופיע פעמיים עם פרופילים שונים, האפליקציה תציג אזהרה.

**בממשק (אחרי טעינה):** מופיעות **התראות עקביות** (באנר + פירוט) — שורות בלי מק״ט, מק״ט חוזר בשורות מרובות, סתירה בין רוחב מוזן לפריסה מפותחת, ועוד. הן **לא חוסמות** יצירת Excel/PDF; מטרתן לעזור לתפוס טעויות לפני הזמנה.

**המספרים בפרופיל הם מידות החתך מלמעלה למטה:**

```
   ┌── 25 ──┐
   │        │ 20
   └──────────────── 170 ──────────────────┘
                                            │ 20
                                   ┌── 25 ──┘
```

- המספרים הזוגיים (1, 3, 5...) = קטעים אופקיים (ימינה)
- המספרים האי-זוגיים (2, 4...) = קטעים אנכיים (למטה)

---

### אפשרות ב׳ — העלאת JSON (למתקדמים)

אם אתה מכין את הנתונים ידנית, צור קובץ JSON בפורמט הזה:

```json
{
  "header": {
    "project_id": "MR277-3",
    "date": "16/03/2026",
    "client_name": "שם הלקוח",
    "project_name": "שם הפרויקט",
    "location": "מיקום אתר",
    "layout_sheet_id": "מזהה_פריסה_חומר",
    "order_number": "12345",
    "color_ral": "9011",
    "material": "אלומיניום אור",
    "thickness_mm": 2
  },
  "panels": [
    {
      "panel_id": "S-101",
      "length_mm": 1570,
      "width_mm": 260,
      "quantity": 6,
      "turn": "N",
      "notes": "",
      "profile_dimensions": [25, 20, 170, 20, 25],
      "bend_angle_deg": 93,
      "bend_offset_mm": 30
    }
  ]
}
```

---

## שלב 2: העלאה לאפליקציה

1. פתח את האפליקציה בנייד או במחשב
2. הקלד **מזהה פרויקט** (למשל: GPN-1)
3. לחץ **"העלאת תמונות שרטוט או JSON"**
4. בחר את התמונה/קובץ — אפשר לבחור כמה תמונות ביחד
5. חכה שהאפליקציה תחלץ את הנתונים (כמה שניות)

---

## שלב 3: בדיקה ועריכה

אחרי שהנתונים חולצו, תראה שתי רשימות לעריכה:

### פרטי הזמנה ✏️
- מזהה פרויקט
- שם לקוח
- שם פרויקט
- תאריך
- צבע RAL (ברירת מחדל: 9011)
- עובי מ"מ (ברירת מחדל: 2)
- מספר הזמנה
- **פריסה חומר (מזהה)** — מומלץ לתיעוד ולדפי פנל
- **מיקום** — מומלץ (אתר / עיר)
- חומר (ברירת מחדל: אלומיניום)

**מלא את מה שחסר!** שדות ריקים יופיעו עם התראה.

**רשימת בקרה לתוצאה עקבית:** [PERFECT_INPUT_CHECKLIST.md](PERFECT_INPUT_CHECKLIST.md) או `PERFECT_INPUT_CHECKLIST.txt` (אותה תיקייה, UTF-8).

### פנלים ✏️
טבלה עם כל הפנלים. אפשר לערוך כל שדה:

| עמודה | הסבר | דוגמה |
|-------|-------|-------|
| מק"ט | מספר זיהוי הפנל | S-101 |
| אורך | אורך במ"מ | 1570 |
| רוחב | ריק = חישוב אוטומטי מפריסה שטוחה (חתך+עובי+כיפוף); מילוי = ערך ידני | (ריק) או 710 |
| כמות | כמה יחידות | 6 |
| סובב | האם לסובב את הפנל (Y/N) | N |
| פרופיל | מידות החתך, מופרדות בפסיקים | 25,20,170,20,25 |
| כל הקטעים בפרופיל | כל אורך שמופיע בחתך (כולל שפת תחתונה / המשך אנכי) חייב להופיע **בעמודת הפרופיל** בפסיקים — האפליקציה **לא ממלאת** מספרים שלא נכתבו. `bend_offset_mm` הוא הערה לשוק / התקנה, לא תוספת אוטומטית לשרשרת |
| הערות | הערות חופשיות | ניקוב |

**אפשר גם להוסיף שורות חדשות** בלחיצה על ה-"+" בתחתית הטבלה.

---

## שלב 4: יצירת הזמנה

1. בדוק את **סיכום ההזמנה** — עמודות **רוחב לתכנון** (מה שנכנס לשטח ולייצוא), **רוחב בטבלה** (מה שהזנת), ו**מצב** (אוטומטי / תואם / בדוק / נעול). מדד **שורות לבדיקת רוחב** מסכם כמה שורות דורשות תשומת לב.
2. לחץ **"צור הזמנה"**
3. חכה כמה שניות — הקבצים נוצרים (ייתכן הודעת toast קצרה)

---

## שלב 5: הורדה ותצוגה

אחרי היצירה תראה:

### כפתורי הורדה
- **📥 Excel** — טבלת הזמנה
- **📥 PDF סיכום** — טבלה מודפסת
- **📥 PDF מלא (חבילה)** — קובץ PDF אחד: סיכום + כל דפי הפנלים (דורש חבילת `requirements` עם `pypdf`)
- **📥 DXF (ZIP)** — כל שרטוטי ה-DXF בזיפ אחד
- **📥 PDF פנלים (ZIP)** — כל שרטוטי הפנלים בזיפ אחד

### תצוגה מקדימה 👁️
לחץ על כל קובץ כדי לראות אותו ישירות באפליקציה:
- **Excel** — מציג את הטבלה
- **PDF סיכום** — מציג את הסיכום
- **PDF פנלים** — מציג כל שרטוט פנל בנפרד, עם כפתורי **הקודם/הבא**
- **DXF** — הורדה בלבד (צריך AutoCAD לפתיחה)

כפתור **"← חזרה לרשימת הקבצים"** מחזיר לרשימה.

---

## ספריית אלמנטים 📚

שמור פנלים כתבניות לשימוש חוזר:

### שמירה
1. אחרי יצירת הזמנה, פתח **"💾 שמור אלמנטים לספריה"**
2. לחץ **"שמור"** ליד כל פנל, או **"שמור הכל"**
3. הפנלים נשמרים בספריה

### שימוש חוזר
1. פתח **"📚 ספריית אלמנטים"** בראש העמוד
2. לחץ **"הוסף"** ליד אלמנט שרוצים להוסיף להזמנה
3. שנה את הכמות/אורך/מידות לפי הצורך

### גיבוי הספריה
- **ייצא ספריה** — שומר קובץ JSON לנייד (גיבוי!)
- **ייבא ספריה** — טוען קובץ JSON חזרה

**חשוב:** גבה את הספריה מדי פעם! הספריה מתאפסת כשהאפליקציה מתעדכנת.

---

## טיפים

- **תמונה לא ברורה?** נסה לצלם שוב עם תאורה טובה יותר
- **המערכת לא מזהה נכון?** ערוך ידנית בטבלת הפנלים
- **מכסת API נגמרה?** השתמש בקובץ JSON במקום תמונה
- **רוצה להתחיל מאפס?** פתח מחדש את הדף
- **שרטוט עם הרבה פנלים?** צלם כל דף בנפרד והעלה את כל התמונות ביחד

---

## מילון מונחים

| מונח | הסבר |
|------|-------|
| פנל | יחידת אלומיניום אחת (לפני כיפוף) |
| פרופיל | צורת החתך של הפנל אחרי כיפוף |
| מידות פרופיל | רצף מספרים שמתאר את החתך (אופקי, אנכי, אופקי...) |
| RAL | קוד צבע תקני (למשל 9011 = שחור) |
| DXF | פורמט שרטוט לתוכנות CAD כמו AutoCAD |
| מק"ט | מספר קטלוגי / מזהה של הפנל |
| כיפוף 93° | זווית הכיפוף (ברירת מחדל: 93 מעלות) |
| פריסה שטוחה | הפנל לפני כיפוף — שטוח על השולחן |
| רוחב פריסה (מחושב) | סכום מסלול החתך + **bend allowance** לפי עובי, K-factor ורדיוס פנימי (מצב מפותח); אפשר `flat_pattern_mode: legacy` לסכום אופקי בלבד |

</div>
