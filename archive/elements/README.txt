archive/elements
================

seed_element_library.json — bundled rows for «ספריית אלמנטים» (same shapes as element_library.json).
Loaded via the app button «טען דוגמאות לארכיון»; duplicates (same panel + profile + length) are skipped.
